# Modal

Displays content that temporarily blocks interactions with the main view.

## ariaLabel

A label describing the Modal's current content. A string. Defaults to `Une modale d'ajout`.

## triggerText

This is the text of the button to trigger a Modal. A string or a React component. Defaults to `Submit`.
