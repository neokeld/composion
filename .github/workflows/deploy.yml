name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Deploy to Github Pages
      run: npm install && npm run deploy -- --ci --host-token-env-variable=VI_NPM_TOKEN
      env:
        VI_NPM_TOKEN: ${{ secrets.GITHUB_TOKEN }}